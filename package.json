{
  "name": "fidelis-channel",
  "version": "0.1.0",
  "description": "Fidelis Protocol Gatekeeper — fiduciary-grade agent authentication channel for Claude Code. Fail-closed Telegram permission relay with policy engine, cryptographic audit logging, and anomaly detection.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "fidelis-channel": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build --silent && node dist/index.js",
    "dev": "tsc --watch",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "keywords": [
    "claude-code",
    "channels",
    "mcp",
    "fidelis-protocol",
    "fiduciary-ai",
    "agent-auth",
    "post-quantum",
    "telegram"
  ],
  "author": "TJ Lane <tj@corpussanctum.org>",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
